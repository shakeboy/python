import math

a = math.sin(90 * 3.1415926 / 180) 		#计算sin45°.
b = math.exp(2)                  		     #计算e2.
c = math.log10(80)                		     #计算log10(80).
d = math.sqrt(18)                 		     #计算 .
sum = a + (b + c) / d              		     #计算整个表达式的值.
print("sum = %8.6f"%sum)
